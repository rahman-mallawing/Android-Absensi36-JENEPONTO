<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.si.uinam.absensi36restfull">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".views.identitywithpagelib.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.checklogpage.ChecklogActivity"></activity>
        <activity android:name=".views.identitywithpagelib.activity.IdentityWithPageActivity" />
        <activity android:name=".views.search.SearchActivity" />
        <activity android:name=".views.monthpresence.MonthPresenceActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>